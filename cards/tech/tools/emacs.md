---
name = 'Emacs'
---

C: Keybinding: [`C-x g`]

Command: [`magit-status`]

---

Q: Command to launch Emacs in TUI mode.
A: `emacs -nw`

---

Q: What does `emacs -nw` do?
A: Launches Emacs in TUI mode.

---

Q: Prefix for user keybindings.
A: `C-c`.

---

Q: What is `C-c` used for?
A: Custom user keybindings.

---

Q: What's the command to turn this:

```
alpha = 1
beta = 2
gamma = 3
```

Into this:

```
alpha = 1
beta  = 2
gamma = 3
```

A: `align-regexp =`

---

C: Keybinding: [`C-End`]

Action: [go to the end of the buffer.]
